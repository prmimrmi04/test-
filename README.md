# Agent Tasks Repository

This repository contains tools and tasks for AI agents.

## Overview
It manages AI agent tasks in `agent_tasks.json`, ensuring task progress is tracked accurately.
It relies on a simple validation script, `scripts/validate_agent_tasks.py` and is fully tested via GitHub Actions.
